/**
 * A simple API to build 3D rendering applications. The jrtr API defines interfaces that 
 * can be implemented using different low-level renderers. Two versions are provided 
 * in the packages {@link jrtr.glrenderer} (a standard OpenGl renderer), and 
 * {@link jrtr.swrenderer} (a software based rasterizer).
 */
package jrtr;
